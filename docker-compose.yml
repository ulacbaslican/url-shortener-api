services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: sqlite:////app/data/urls.db
    volumes:
      - db_data:/app/data
    restart: unless-stopped

volumes:
  db_data:
